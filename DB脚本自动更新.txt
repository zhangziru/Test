思想：
	1、通过程序实现DB脚本自动更新，将程序中与数据库交互的脚本存到数据库中。
	2、目的：是为了防止脚本重复执行。
实现方式：
	1、数据库中创建 S_SqlScript_Log(系统SQL脚本日志)表。
		1）涉及4个关键字段：脚本键（Key,唯一）、脚本备注（Comment）、脚本的内容（SQLContent）、标记是否执行（IsExecuted）
		2）非关键字段：执行人、执行时间、更新人、更新时间
	2、通过判断脚本键是否在 系统SQL脚本日志表 中存在，不存在 => 插入 SQL脚本日志。
	3、判断是否已执行 => 来确定是否执行脚本。